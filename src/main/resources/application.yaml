spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/Shop
    username: postgres
    password: 13122002
  main:
    allow-circular-references: true
  mvc:
    hiddenmethod:
      filter:
        enabled: true